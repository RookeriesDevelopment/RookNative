# RookNative

Demo app for rook-sdk and rook-sdk-samsung.

## Configuration

In the root folder create a `local.properties` file with the following properties:

```properties
clientUUID=CLIENT_UUID
secretKey=SECRET_KEY
```
